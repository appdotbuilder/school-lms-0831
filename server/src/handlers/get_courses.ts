import { type Course } from '../schema';

export async function getCourses(): Promise<Course[]> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is fetching all courses from the database.
  // Returns all available courses, used by students to browse courses they can enroll in.
  return [];
}